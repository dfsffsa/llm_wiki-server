# 开发交接 — 2026-08-02

> 本文件是跨机器交接：上一个会话（worktree `feat+audit-log`）完成了 **站内审计 + 中英 i18n**，并和 main 上的 **billing（Waffo 支付）** 合并。全部已推送，分支已清理。**下一台电脑开发前先读这份 + CLAUDE.md。**

## 1. 仓库当前状态（已全部合并推送）

- **main = `8aa2471`**，本地与 origin/main 一致，工作树干净。
- main 现在包含三大功能：
  1. **billing（Waffo Pancake 支付）** — `overlay/server/src/api/billing.rs`，checkout + webhook，见 [付款-Waffo-Pancake.md](./付款-Waffo-Pancake.md)
  2. **站内审计** — `overlay/server/src/audit.rs`，`LLM_WIKI_AUDIT_DIR` 启用 JSONL 访问日志，见 [站内审计.md](./站内审计.md)
  3. **中英 i18n** — `overlay/static/i18n.js`（**唯一文案源**）+ 5 个公开页面 `data-i18n`，见 [国际化-i18n.md](./国际化-i18n.md)
- 分支已全部清理：只剩 main（feat/llm-judge-phase2、feat/public-deploy-auth、worktree-feat+audit-log 均已删除）。

## 2. 新机器上手（第一条命令）

```bash
git clone --recurse-submodules git@github.com:dfsffsa/llm_wiki_server.git
# 依赖：cargo、node ≥20、rustup musl target、protoc ≥3.21
npm ci --prefix upstream && npm ci --prefix overlay/cli/node
./scripts/build-all.sh   # 应无错误
```

完整清单见 CLAUDE.md 的 "Quick Onboarding Checklist"。

## 3. 立即要做（按优先级）

1. **编译 + 部署 merged main 到两台服务器**（代码已合并，但**还没部署**）：
   - server 需 musl 静态交叉编译（audit/billing/i18n 都在 server 里）
   - **`VITE_BACKEND=http VITE_API_TOKEN=... ./scripts/build-web.sh`** —— lite 的 i18n 改动**必须重建 dist 才进部署**（lite 是构建产物）
   - `SSH_HOST=... deploy-ecs.sh`，可选设 `SERVER_AUDIT_DIR` 启用站内审计
2. **线上验证 i18n**：两个域名 `/`、`/login`、`/register`、`/lite/` 中英文切换（右上角/导航 `🌐 EN` / `🌐 中`）
3. **线上验证 billing**：Pro 订阅按钮 → `/api/v1/billing/checkout` → 支付跳转
4. **审计跑起来**：deploy 时 `SERVER_AUDIT_DIR` 注入 systemd，之后 `scripts/audit-summary.sh ecs99|ecs199` 看访客汇总

## 4. 关键注意事项（上一会话踩过的坑）

| 事项 | 说明 |
|------|------|
| **upstream 子模块 dirty 是正常的** | `apply-patches.sh` 后的状态；**不要提交子模块指针** |
| **server 编译慢（LTO）** | release profile `-C lto` + `codegen-units=1`，改 1 行 server 源码也要 **7-10 分钟**重链接。多次 server 改动攒批一次编译；纯前端改动（overlay/static）用旧二进制即可实测 |
| **静态文件缓存 1h** | 除 i18n.js（已 no-cache）外，静态文件 `Cache-Control: max-age=3600`。改 landing HTML 等要等缓存过期才生效 |
| **改文案只改 i18n.js** | 文案唯一源在 `overlay/static/i18n.js` 的 `COPY` 字典；改品牌名/slogan/按钮 = 改对应 key 一行，刷新即生效（见 docs/国际化-i18n.md §2） |
| **lite 是构建产物** | 改 `overlay/static/lite/` 后必须 `build-web.sh` 才进部署 |
| **密钥不硬编码** | LLM key / SMTP_PASS 走占位符注入 `server.local.json`（chmod 600）；脚本只读 env |
| **wiki 数据不在 repo** | 在 `~/overseas-github/llm_wiki_projects/` |
| **双服务器 SSH 不同** | ecs99（阿里云）：端口 22022、root；ecs199（腾讯）：li 用户无 sudo，`SKIP_SYSTEMD=1` 手动装 unit |

## 5. 相关文档

- [站内审计.md](./站内审计.md) — 访问日志、audit-summary.sh、部署（SERVER_AUDIT_DIR）
- [国际化-i18n.md](./国际化-i18n.md) — i18n 机制、改文案工作流、踩坑记录
- [付款-Waffo-Pancake.md](./付款-Waffo-Pancake.md) — billing 配置、e2e-billing、runbook
- [部署实战与排错记录-2026-08-02.md](./部署实战与排错记录-2026-08-02.md) — 双服务器部署实测
- [部署-低配ECS一键脚本.md](./部署-低配ECS一键脚本.md) — deploy-ecs.sh / sync-artifacts.sh
